meta {
  name: Create Policy
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/iam/policies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "custom:resource:read",
    "description": "Read access to custom resource",
    "effect": "ALLOW",
    "actions": ["custom:resource:read"],
    "resources": ["*"]
  }
}

script:post-response {
  bru.setEnvVar("policyId", res.body.data.id);
}
