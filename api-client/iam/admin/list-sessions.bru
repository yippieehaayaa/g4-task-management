meta {
  name: List Sessions (Admin)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/iam/admin/identities/{{identityId}}/sessions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
