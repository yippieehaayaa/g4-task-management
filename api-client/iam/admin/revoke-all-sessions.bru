meta {
  name: Revoke All Sessions (Admin)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/iam/admin/identities/{{identityId}}/sessions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
