meta {
  name: Expire OTPs (Admin)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/iam/admin/identities/{{identityId}}/otps/expire
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
