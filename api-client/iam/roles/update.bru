meta {
  name: Update Role
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/iam/roles/{{roleId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "editor",
    "description": "Updated description"
  }
}
