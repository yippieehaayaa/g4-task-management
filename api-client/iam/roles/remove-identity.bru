meta {
  name: Remove Identity from Role
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/iam/roles/{{roleId}}/identities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "identityId": "{{identityId}}"
  }
}
