meta {
  name: Add Roles to Group
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/iam/groups/{{groupId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "roleIds": ["{{roleId}}"]
  }
}
