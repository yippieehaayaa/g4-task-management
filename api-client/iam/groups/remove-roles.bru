meta {
  name: Remove Roles from Group
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/iam/groups/{{groupId}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "roleIds": ["{{roleId}}"]
  }
}
