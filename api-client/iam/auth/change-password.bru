meta {
  name: Change Password
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/iam/auth/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currentPassword": "Admin@1234",
    "newPassword": "Admin@5678"
  }
}
