meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/iam/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}
