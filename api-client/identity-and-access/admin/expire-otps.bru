meta {
  name: Expire OTPs (Admin)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/identities/{{identityId}}/otps/expire
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
