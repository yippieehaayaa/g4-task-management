meta {
  name: Revoke Session (Admin)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/identities/{{identityId}}/sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
