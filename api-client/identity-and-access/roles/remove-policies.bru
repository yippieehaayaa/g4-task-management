meta {
  name: Remove Policies from Role
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/roles/{{roleId}}/policies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "policyIds": ["{{policyId}}"]
  }
}
